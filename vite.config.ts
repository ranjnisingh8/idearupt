import { defineConfig } from "vite";
import react from "@vitejs/plugin-react-swc";
import path from "path";
import { componentTagger } from "lovable-tagger";
import { VitePWA } from "vite-plugin-pwa";

// https://vitejs.dev/config/
export default defineConfig(({ mode }) => ({
  server: {
    host: "::",
    port: 8080,
    hmr: {
      overlay: false,
    },
  },
  plugins: [
    react(),
    mode === "development" && componentTagger(),
    VitePWA({
      registerType: "autoUpdate",
      includeAssets: ["favicon.ico", "robots.txt"],
      workbox: {
        // SPA: serve index.html for all navigation requests
        navigateFallback: "/index.html",
        // Don't intercept Supabase, API, or non-SPA routes
        navigateFallbackDenylist: [/^\/api/, /^\/auth\/callback/, /^\/sw\.js$/],
        // Only precache core assets — avoid caching API responses
        globPatterns: ["**/*.{js,css,html,ico,png,svg,woff2}"],
        // Skip waiting and claim immediately for seamless updates
        skipWaiting: true,
        clientsClaim: true,
        // Limit cache size to prevent stale assets
        runtimeCaching: [
          {
            urlPattern: /^https:\/\/fonts\.googleapis\.com\/.*/i,
            handler: "CacheFirst",
            options: {
              cacheName: "google-fonts-cache",
              expiration: { maxEntries: 10, maxAgeSeconds: 60 * 60 * 24 * 365 },
              cacheableResponse: { statuses: [0, 200] },
            },
          },
        ],
      },
      manifest: {
        name: "Idearupt — Find Problems Worth Solving",
        short_name: "Idearupt",
        description: "AI-powered startup problems backed by real complaints from Reddit & Hacker News.",
        theme_color: "#08090E",
        background_color: "#08090E",
        display: "standalone",
        orientation: "portrait",
        scope: "/",
        start_url: "/",
        icons: [
          {
            src: "/pwa-192x192.png",
            sizes: "192x192",
            type: "image/png",
          },
          {
            src: "/pwa-512x512.png",
            sizes: "512x512",
            type: "image/png",
          },
          {
            src: "/pwa-512x512.png",
            sizes: "512x512",
            type: "image/png",
            purpose: "maskable",
          },
        ],
      },
    }),
  ].filter(Boolean),
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
}));
